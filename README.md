A virtual smartqueue app that creates a queue by issuing tokens and deleting tokens.
SQLite is used as database along with Django framework.
Has authentication inbuilt with whole administration control.
